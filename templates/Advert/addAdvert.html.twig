
{%  extends 'base.html.twig' %}

{%  block body %}

{% form_theme form 'jquery.collection.html.twig' %}

{# affiche la balise d'ouverture du formulaire HTML, soit<form> #}
{{ form_start(form) }}
{# form_row(form.date) #}
{# form_row() //affiche le label, les erreurs et le champ en même temps,
en respectant la vue définit dans le thème du formulaire que vous utilisez. #}
{{ form_row(form.title) }}
{# form_errors() //affiche les erreurs attachées au champ donné en argument. #}
{# form_errors(form.title) #}

{# form_label() //affiche le label HTML du champ donné en argument. Le deuxième argument est le contenu du label. #}
{# form_label(form.content, "Contenu de l'annonce", {'label_attr': {'class': 'col-sm-2 control-label'}}) #}

{# form_widget() // Affiche le champ HTML lui-même (que ce soit<input>,<select>, etc.).#}
{# form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) #}

{# Pour le bouton, pas de label ni d'erreur, on affiche juste le widget #}
{# form_widget(form.save, {'attr': {'class': 'btn btn-primary'}}) #}

{# form_rest() affiche tous les champs manquants du formulaire #}


{{ form_widget(form.advertSkills, {'attr' :{'class' : 'my-selector'}}) }}
{{ form_label(form.Categories, 'Categories ')}}
{{ form_widget(form.Categories, {'attr' :{'class' : 'my-selector'}}) }}


{{ form_rest(form) }}

{# affiche la balise de fermeture du formulaire HTML, soit </form>. #}
{{ form_end(form) }}

{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/jquery.js') }}"></script>
    <script src="{{ asset('js/jquery.collection.js') }}"></script>

    <script type="text/javascript">
        $('.my-selector').collection();
    </script>

{% endblock %}
